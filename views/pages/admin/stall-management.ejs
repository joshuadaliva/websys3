<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>ARKIPAISI &mdash; Stall Management</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=DM+Mono:wght@400;500&display=swap"
    rel="stylesheet" />
  <link rel="stylesheet" href="/css/admin/stall-management.css">
</head>

<body>

  <%- include("../../partials/sidebar.ejs", { currentRoute, role: "admin" , initials: "admin" ,fullName: "joshua daliva" })
    %>


    <!-- MAIN -->
    <div class="main" id="main">
      <%- include("../../partials/header.ejs", {role: "admin" , initials: "admin" , lastName: "daliva" , }) %>


        <div class="content">
          <div class="pghdr">
            <div>
              <h1>Stall Management</h1>
              <p>Pili Public Market Building (PPMB-23) &mdash; Click any stall to view details or post vacant stalls</p>
            </div>
          </div>

          <!-- Summary strip — server-rendered counts -->
          <div class="sum-strip">
            <div class="sum-card">
              <div class="sum-val" style="color:var(--ac)">
                <%= summary.total %>
              </div>
              <div class="sum-lbl">Total Stalls</div>
            </div>
            <div class="sum-card">
              <div class="sum-val" style="color:var(--gr)">
                <%= summary.occupied %>
              </div>
              <div class="sum-lbl">Occupied</div>
            </div>
            <div class="sum-card">
              <div class="sum-val" style="color:var(--t3)">
                <%= summary.vacant %>
              </div>
              <div class="sum-lbl">Vacant</div>
            </div>
            <div class="sum-card">
              <div class="sum-val" style="color:var(--rd)">
                <%= summary.overdue %>
              </div>
              <div class="sum-lbl">Overdue</div>
            </div>
            <div class="sum-card">
              <div class="sum-val" style="color:var(--ac)">
                <%= summary.posted %>
              </div>
              <div class="sum-lbl">Publicly Posted</div>
            </div>
          </div>

          <!-- View Tabs -->
          <div class="view-tabs">
            <div class="view-tab on" id="vtab-map" onclick="setView('map',this)">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                stroke-width="2">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
                <polyline points="9 22 9 12 15 12 15 22" />
              </svg>
              Stall Map
            </div>
            <div class="view-tab" id="vtab-posted" onclick="setView('posted',this)">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                stroke-width="2">
                <path d="M22 2L11 13" />
                <polygon points="22 2 15 22 11 13 2 9 22 2" />
              </svg>
              Publicly Posted
              <span class="vtab-cnt" id="postedTabCount">
                <%= summary.posted || '' %>
              </span>
            </div>
          </div>

          <!-- MAP VIEW -->
          <div id="view-map">
            <div class="floor-tabs">
              <div class="floor-tab on" onclick="setFloor('gf',this)">Ground Floor</div>
              <div class="floor-tab" onclick="setFloor('sf',this)">Second Floor</div>
            </div>
            <!-- Ground Floor -->
            <div id="floor-gf">
              <div class="map-container">
                <div class="map-header">
                  <div class="map-title">Ground Floor &mdash; PPMB-23</div>
                  <div class="map-legend">
                    <div class="map-legend-item" data-tip="Paid · Green indicator">
                      <div class="map-legend-swatch" style="background:var(--grl);border:2px solid var(--grs)"></div>
                      Paid
                    </div>
                    <div class="map-legend-item" data-tip="Overdue · Red indicator">
                      <div class="map-legend-swatch" style="background:var(--rdl);border:2px solid var(--rds)"></div>
                      Overdue
                    </div>
                    <div class="map-legend-item" data-tip="Partial · Amber indicator">
                      <div class="map-legend-swatch" style="background:var(--aml);border:2px solid var(--ams)"></div>
                      Partial
                    </div>
                    <div class="map-legend-item" data-tip="Vacant · Gray indicator">
                      <div class="map-legend-swatch" style="background:var(--s3);border:2px solid var(--brd)"></div>
                      Vacant
                    </div>
                    <div class="map-legend-item" data-tip="Posted · Blue accent indicator">
                      <div class="map-legend-swatch" style="background:var(--acl);border:2px solid var(--acs)"></div>
                      Posted
                    </div>
                  </div>
                </div>
                <div class="map-body">
                  <div class="floor-plan" id="gfPlan"></div>
                </div>
              </div>
            </div>
            <!-- Second Floor -->
            <div id="floor-sf" style="display:none">
              <div class="map-container">
                <div class="map-header">
                  <div class="map-title">Second Floor &mdash; PPMB-23</div>
                  <div class="map-legend">
                    <div class="map-legend-item" data-tip="Paid · Green indicator">
                      <div class="map-legend-swatch" style="background:var(--grl);border:2px solid var(--grs)"></div>
                      Paid
                    </div>
                    <div class="map-legend-item" data-tip="Overdue · Red indicator">
                      <div class="map-legend-swatch" style="background:var(--rdl);border:2px solid var(--rds)"></div>
                      Overdue
                    </div>
                    <div class="map-legend-item" data-tip="Partial · Amber indicator">
                      <div class="map-legend-swatch" style="background:var(--aml);border:2px solid var(--ams)"></div>
                      Partial
                    </div>
                    <div class="map-legend-item" data-tip="Vacant · Gray indicator">
                      <div class="map-legend-swatch" style="background:var(--s3);border:2px solid var(--brd)"></div>
                      Vacant
                    </div>
                    <div class="map-legend-item" data-tip="Posted · Blue accent indicator">
                      <div class="map-legend-swatch" style="background:var(--acl);border:2px solid var(--acs)"></div>
                      Posted
                    </div>
                  </div>
                </div>
                <div class="map-body">
                  <div class="floor-plan" id="sfPlan"></div>
                </div>
              </div>
            </div>
          </div><!-- /view-map -->

          <!-- PUBLICLY POSTED VIEW -->
          <div id="view-posted" style="display:none">
            <div
              style="display:flex;align-items:center;justify-content:space-between;margin-bottom:18px;flex-wrap:wrap;gap:12px">
              <div>
                <div style="font-size:16px;font-weight:800;letter-spacing:-.3px">Publicly Posted Stalls</div>
                <div style="font-size:13px;color:var(--t3);margin-top:3px">Vacant stalls open for vendor applications
                </div>
              </div>
            </div>
            <div class="posted-grid" id="postedGrid"></div>
          </div>
        </div>
    </div><!-- /main -->

    <!-- DETAIL DRAWER -->
    <div class="detail-overlay" id="detailOverlay" onclick="closeDetail()"></div>
    <div class="detail-panel" id="detailPanel">
      <div class="dp-head">
        <div class="dp-title" id="dpTitle">Stall Details</div>
        <div class="dp-x" onclick="closeDetail()"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"
            fill="none" stroke="currentColor" stroke-width="2">
            <line x1="18" y1="6" x2="6" y2="18" />
            <line x1="6" y1="6" x2="18" y2="18" />
          </svg></div>
      </div>
      <div class="dp-body" id="dpBody"></div>
      <div class="dp-foot" id="dpFoot"></div>
    </div>

    <div class="toast" id="toast"></div>

    <%# Inject server-side stall data and listing details as JS variables — mirrors dashboard pattern %>
      <script>
        var STALLS = <%- JSON.stringify(stalls) %>;
        var STALL_LISTINGS = <%- JSON.stringify(stallListings) %>;
      </script>

      <script src="/js/admin/stall-management.js"></script>
</body>

</html>